# Plan: prj-434-task-mugu4j6t

**Project:** `PRJ-3759`  
**Task ID:** `task-mugu4j6t`  
**Repo:** `prj-434-task-mugu4j6t`  

## Summary

Декомпозирую задачу: 4 этапа — репо и CLI каркас, парсинг CSV→JSON, тесты, документация и финализация.

## Subtasks

### 1. Создать репозиторий и CLI каркас

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать новый публичный репозиторий prj-434-task-mugu4j6t на GitHub, инициализировать проект Python (pyproject.toml/poetry или pip+venv), добавить точку входа CLI с argparse: аргументы --input/-i (путь к CSV), --output/-o (путь к JSON, опционально — stdout если не указан), --delimiter/-d (по умолчанию запятая), --encoding/-e (по умолчанию utf-8), --indent (по умолчанию 2), --pretty/--minified. Структура: csv2json/__init__.py, csv2json/cli.py, csv2json/__main__.py (python -m csv2json). Без бизнес-логики — только парсинг аргументов и вызов заглушки converter.convert(). requirements.txt + README-заглушка.
- **Test plan:** git clone https://github.com/ZhenyaRUS39/prj-434-task-mugu4j6t; cd в репо; python -m csv2json --help выводит все аргументы; python -m csv2json (без аргументов) падает с понятной ошибкой и exit code != 0; pyproject.toml валиден (python -m pip install -e . проходит).

### 2. Реализовать конвертер CSV→JSON

- **ID:** `sub-2`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** В csv2json/converter.py реализовать чистую функцию convert(input_path, output_path=None, *, delimiter=',', encoding='utf-8', indent=2, pretty=True) -> list[dict]. Чтение CSV через csv.DictReader, обработка пустых строк (skip), BOM (utf-8-sig), кавычек и экранирования (встроенный csv-модуль справляется). Запись JSON: если output_path=None — в stdout (sys.stdout.write(json.dumps(data, indent=indent, ensure_ascii=False) + '\n')), иначе — в файл. Ошибки: FileNotFoundError, PermissionError, UnicodeDecodeError → понятные сообщения в stderr и exit code 2. CLI из sub-1 вызывает эту функцию и маппит исключения на коды возврата.
- **Test plan:** Создать fixtures/sample.csv с 5+ строк (включая кириллицу, поле в кавычках с запятой внутри, пустую строку); python -m csv2json -i fixtures/sample.csv выводит валидный JSON в stdout; python -m csv2json -i fixtures/sample.csv -o out.json && python -c "import json; json.load(open('out.json'))" не падает; python -m csv2json -i nonexistent.csv возвращает exit code 2 и пишет в stderr; --delimiter ';' корректно парсит CSV с точкой с запятой.
- **Dependencies:** `sub-1`

### 3. Написать автотесты (pytest)

- **ID:** `sub-3`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** tests/test_converter.py: параметризованные тесты на convert() — минимум 8 кейсов: обычный CSV, пустой CSV (только заголовок), CSV с BOM, CSV с кавычками и запятыми внутри полей, CSV с кириллицей, CSV с разделителем ;, CSV с пустыми значениями (None), CSV с переносами строк внутри полей. tests/test_cli.py: интеграционные тесты через subprocess + click.testing или прямой вызов argparse — минимум 5 кейсов: --help, базовая конвертация в файл, stdout-режим, несуществующий файл, кастомный разделитель. Покрытие converter.py и cli.py ≥ 90% (pytest-cov). Конфиг pytest в pyproject.toml [tool.pytest.ini_options]. Фикстуры в tests/conftest.py. CI не настраиваем (отдельный этап).
- **Test plan:** python -m pytest -v показывает все тесты зелёными; python -m pytest --cov=csv2json --cov-report=term-missing показывает coverage ≥ 90% для csv2json/; тесты изолированы (tmp_path для файлов), не зависят от CWD; python -m pytest --tb=short проходит за < 5 секунд.
- **Dependencies:** `sub-2`

### 4. Документация, примеры и финальный коммит

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** README.md: описание, установка (pip install -e .), использование с примерами (все флаги CLI), таблица кодов возврата (0=ok, 1=ошибка парсинга аргументов, 2=ошибка ввода-вывода). .gitignore (Python-стандарт + __pycache__, .pytest_cache, *.egg-info, .coverage, htmlcov/). examples/sample.csv (5 строк реалистичных данных) + examples/out.json (ожидаемый результат). CHANGELOG.md (v0.1.0). Линт: ruff check . (если добавим) или хотя бы flake8 csv2json/. Все файлы закоммичены одним коммитом "feat: initial CSV→JSON CLI implementation" в main. Репо публичное, ветка main — источник правды.
- **Test plan:** README рендерится корректно на GitHub; examples/ примеры воспроизводимы (python -m csv2json -i examples/sample.csv даёт examples/out.json); git log -1 показывает коммит с ожидаемым сообщением; репозиторий публичный (видно без авторизации); .gitignore отрабатывает (python -m csv2json создаёт __pycache__/, но он не закоммичен).
- **Dependencies:** `sub-3`


---
*Generated by Hermes Orchestrator*
